# 체크리스트: 20260427_Vercel_빌드_타입_오류_수정

## 1. 에러 분석 및 원인 파악
- [x] Vercel 빌드 로그 분석: `images.formats` 타입 불일치 확인 ✅
- [x] `next.config.ts` 현재 상태 확인 ✅

## 2. 코드 수정 (최소 침습)
- [x] `next.config.ts` 수정 ✅
    - [x] `import type { NextConfig } from 'next'` 추가 ✅
    - [x] `nextConfig` 변수에 `NextConfig` 타입 명시 ✅
    - [x] `formats` 배열의 타입 추론 문제 해결 ✅

## 3. 검증 및 배포
- [x] 로컬 빌드 테스트 (`npm run build`) ✅
- [ ] Vercel 재배포 요청 (GitHub Push 시 자동 수행 예정)

## 4. 문서 현행화
- [x] `MEMORY.md` 및 `SYSTEM_MAP.md`에 수정 사항 기록 ✅
