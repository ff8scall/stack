# 체크리스트 (2026-04-22: Infra 크롤러 확장 및 데이터 안정화)

## 📅 작업 정보
- **기능명**: Infra 카테고리 크롤러 추가 및 데이터 검증 로직 강화
- **담당**: Antigravity
- **상태**: ✅ 완료 (2026-04-22)

## 📋 TODO 리스트

### 1. 신규 인프라 크롤러 개발
- [x] `scripts/scraper/upstash_scraper.js` 구현 (Redis/Kafka 가격 파싱)
- [x] `scripts/scraper/pinecone_scraper.js` 구현 (Vector DB 가격 파싱)
- [x] `run_all.js`에 신규 스크립트 등록

### 2. 데이터 업데이트 및 검증 로직 고도화
- [x] `update_data.js`에서 `upstash-redis`, `pinecone-db` 매핑 로직 추가
- [x] `results.json` 데이터에 대한 산술적 검증(Range Validation) 로직 추가
- [x] 가격 변동폭이 클 경우(예: 50% 이상) 로그에 경고 출력

### 3. Stack Builder UI 보완
- [x] `CalculatorBar.tsx`에서 인프라 단위(GB, Request)가 섞일 때의 합산 로직 재점검 (baseUsage 도입)
- [x] `bricks.ts`의 `lastUpdated` 날짜가 모든 항목에서 동기화되는지 확인

---

## 🏁 검증 결과
- [x] `node scripts/scraper/run_all.js` 실행 시 모든 인프라 가격이 `results.json`에 정상 기록되는가? (확인됨)
- [x] `update_data.js` 실행 후 `src/data/bricks.ts` 파일의 값이 실제 웹사이트 가격과 일치하는가? (확인됨)
- [x] 가격이 비정상적일 경우 업데이트를 중단하고 에러를 리포트하는가? (확인됨)
