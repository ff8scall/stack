# 체크리스트: SEO 및 메타데이터 최적화

## 📋 작업 개요
- **목표**: Next.js 16 아키텍처 내에서 검색 엔진 최적화(SEO) 및 메타데이터 정합성을 확보하고, 시각적 계층 구조(H1)를 개선한다.
- **담당**: Antigravity (AI Coding Assistant)

## 🛠️ 세부 작업 항목

### 1. 전역 메타데이터 고도화
- [x] `src/app/[locale]/layout.tsx` 내 `viewport` 설정 추가 (mobile-first)
- [x] 파비콘(Favicon) 및 애플 터치 아이콘 관련 메타 데이터 보충
- [x] `theme-color` 설정 (심미적 다크 모드 테마 반영)

### 2. 시각적 계층 구조 (H1) 개선
- [x] `src/components/Navbar.tsx` 로고 영역의 SEO 가이드 준수 (H1 중복 방지 위해 div 유지)
- [x] 메인 페이지(`page.tsx`) 최상단에 검색 엔진용 시맨틱 `<h1>` 추가 (t('title') 활용)
- [x] 의미 없는 `div` 대신 시맨틱 태그(`main`, `header`) 사용 확인

### 3. 중복 및 누락 점검 (Review)
- [x] Metadata API로 단일화하여 중복 태그 방지
- [x] 각 다국어 페이지(`/ko`, `/en`)의 `lang` 속성 및 `canonical` URL 설정 완료
- [x] OpenGraph 및 Twitter Card 데이터 보강

### 4. 최종 검증
- [ ] 빌드 후 메타 데이터 노출 여부 확인
- [ ] Lighthouse 또는 유사 도구를 통한 SEO 점수 체크 시뮬레이션

---
## 📅 진행 상태
- **시작일**: 2026-04-22
- **현재 진행률**: 0% (기획 완료)
- **최종 완료일**: TBD
